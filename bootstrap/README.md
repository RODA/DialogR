# DialogR Bootstrap

This folder registers DialogR as a product extension of the base app.

It should compose product metadata, dialogs, menus, capabilities, product translations, and startup behavior.

It must not contain feature logic.
